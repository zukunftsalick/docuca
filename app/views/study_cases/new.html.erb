<h1>New Study Case</h1>

<%= error_messages_for :study_case %>

<% form_for(:study_case, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>
<br/>
<%= link_to 'Back', collection_url %>