<% @comments.each do |comment| %>
	<%= image_tag( 'user_comment.png', :alt=> 'Comentário')%> 
	<%=h comment.comment %><br>
<% end%>
<hr/>

<div id='comment_form'>
<% remote_form_for(
  :comment, 
  :url => object_url
 )  do |f| %>
  	<%= render :partial => "form", :locals => { :f => f } %>
	  <%= submit_tag "Enviar"%>
<%end%>
</div>
